import React from "react"

export default function Add(props)
{
    let a=props.num1
    let b=props.num2

    let sum=a+b

    return <div>
        {/* <p>Sum of {a} + {b}={sum}</p> */}

        

    </div>
}